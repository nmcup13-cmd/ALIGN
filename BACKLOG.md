# BACKLOG.md — สิ่งที่ยังไม่เสร็จ (ส่งต่อ Module 3)

> สรุปจากสถานะโค้ดจริง ณ 2026-09-19 (ดู `spec.md` สำหรับรายละเอียดสเปกทั้งหมด, `test-results.md` สำหรับผลทดสอบ) — ไฟล์นี้บอกเฉพาะ**ส่วนที่ยังไม่เสร็จ**และเหตุผล เพื่อให้ Module ถัดไปตัดสินใจต่อได้โดยไม่ต้องไล่อ่านโค้ดทั้งหมดใหม่

## งานที่ทำเสร็จแล้วในรอบนี้ (บริบทสั้นๆ ก่อนดู backlog)
CLO/PLO management เต็มรูปแบบ (รวม PLO CRUD ที่เพิ่งเสร็จ), AI matching agent แบบ agentic พร้อม audit log, การยืนยันผลโดยอาจารย์ (กฎ #3), Course Syllabus entry, แผนที่ CLO×สัปดาห์, แดชบอร์ดทั้ง 2 บทบาท, Word export ทั้งระดับวิชา/หลักสูตร, **แนบไฟล์หลักฐานจริงผ่าน Vercel Blob พร้อม PDPA access log** (เพิ่งเสร็จ ปิดช่องว่างเดิมที่เคยติด Firebase Blaze plan)

---

## รายการที่ยังไม่เสร็จ

### 1. AI วิเคราะห์ gap เทียบ syllabus แบบมีโครงสร้าง (AB-22)
**สถานะ**: ทำได้บางส่วน — agent อ่าน syllabus จริงแล้ว (ปิดช่องว่างที่เคยว่างเปล่าตลอด) แต่ผลลัพธ์ปนอยู่ในข้อความสรุปอิสระ ไม่มีเอาต์พุตแยกชัดเจนเป็น "หัวข้อที่ขาด" vs "หัวข้อที่สอนเพิ่ม" ตามที่ backlog เดิมตั้งใจ
**ถ้าจะทำต่อ**: เพิ่ม collection `syllabus_gap_results` (auto id, append-only ตามที่ align-api-schema-design.md §3.11 เคยออกแบบไว้), ปรับ prompt ของ `runCloMatchAgent` (`src/lib/ai/openrouter.ts`) ให้คืนค่าเป็น structured JSON แยก 2 รายการแทนข้อความอิสระ

### 2. ไฟล์ syllabus ฉบับทางการ (official file upload)
**สถานะ**: ยังไม่ทำ — ตอนนี้ทำแค่ "หัวข้อรายสัปดาห์" (ข้อความ) ที่ `/courses/[curriculumId]/[code]/syllabus`
**ทำได้ทันทีถ้าต้องการ**: โครงสร้าง Vercel Blob (private access) มีอยู่แล้วจากฟีเจอร์แนบหลักฐาน (`src/app/evidence/[evidenceId]/download/route.ts` เป็น pattern อ้างอิงได้ตรงๆ) — แค่ยังไม่ได้ต่อยอดมาที่หน้า syllabus

### 3. ระบบแจ้งเตือนอัตโนมัติ (Notification, AB-12/AB-23)
**สถานะ**: **ตัดสินใจไม่ทำแยกแล้ว** (ยืนยันโดยเจ้าของโปรเจกต์ 2026-09-19) — ใช้หน้าที่มีอยู่แล้วแสดง gap แทน (หน้าจัดการรายวิชา/แดชบอร์ด/แผนที่ CLO×สัปดาห์)
**เหตุผลที่ไม่ทำ**: โปรเจกต์นี้ไม่มี Cloud Functions/background job ให้ trigger แบบ "ทันที" ตามที่สเปคเดิมต้องการ
**ถ้า Module 3 อยากทำจริง**: แนะนำ Vercel Cron Job (ไม่ใช่ Firebase Cloud Functions) — แต่ Hobby plan รันได้แค่วันละครั้ง ไม่ใช่ real-time ต้องยอมรับข้อจำกัดนี้หรืออัปเกรด Vercel plan

### 4. บทบาทที่ 3 (QA / ผู้ประเมินภายนอก)
**สถานะ**: ตั้งใจไม่ทำ — ตามสเปคเดิม QA ไม่มี login เข้าระบบเอง ได้รับแค่เอกสาร Word ที่ export ออกไป (AB-17) ถือว่า "เสร็จ" ตามเจตนาเดิมแล้ว ไม่ใช่ของค้าง

### 5. เกณฑ์ "บรรลุ PLO" แบบผ่าน/ไม่ผ่าน
**สถานะ**: ตั้งใจไม่ทำ — ไม่มีสูตรตายตัวในสเปกต้นทาง ระบบแสดงเป็นสัดส่วน % เท่านั้น ถ้า Module 3 ต้องการเกณฑ์ชัดเจน ต้องนิยามสูตรใหม่เอง (ไม่ใช่การเดาต่อจากโค้ดเดิม)

### 6. PDF export
**สถานะ**: ไม่มีในสเปกที่ไหนเลยตั้งแต่ต้น ระบบ export เป็น Word (.docx) เท่านั้นตามที่ยืนยันแล้ว

### 7. มุมมองรวมข้ามหลักสูตร 2565↔2570
**สถานะ**: ห้ามโดยเด็ดขาดตามกฎ #1 ของโปรเจกต์ — ไม่ใช่ของค้าง เป็นข้อจำกัดตั้งใจถาวร

---

## สรุปสำหรับ Module 3
ถ้าต้องเลือกทำต่อ เรียงตามความคุ้มค่า: **(1) syllabus gap แบบมีโครงสร้าง** (ข้อมูลพร้อมแล้ว แค่ปรับ prompt+schema) → **(2) ไฟล์ syllabus ทางการ** (infra พร้อมแล้วจาก Vercel Blob) → ส่วนที่เหลือเป็นการตัดสินใจเชิงสเปกที่ต้องคุยกับผู้ใช้ก่อน ไม่ใช่งานเทคนิคล้วนๆ
