# SCOPE.md — โครงสร้างข้อมูลของระบบแผนการสอน

ตารางนี้เทียบโครงสร้างของแอปตัวอย่าง (LeaveEasy) กับระบบของฉัน
เก็บไว้ใช้ตลอด 4 สัปดาห์ของการบ้าน

> **หมายเหตุที่มาของเนื้อหา (2026-09-06)**: ไฟล์นี้รวม 2 เวอร์ชันไว้ด้วยกันเพื่อเทียบกันได้ง่าย — (1) ตารางต้นฉบับจากโปรเจกต์การบ้านแยกต่างหาก
> `align-homework-w6` (Firebase project `align-e7d99`, repo `nanesay`) ซึ่ง **เป็นคนละระบบกับ ALIGN vault นี้โดยเจตนา** และ (2) ตารางที่ sync ชื่อ entity ให้ตรงกับ
> schema จริงของ ALIGN (`nmc-align-2026`, [[docs/02-design/02-technical/align-api-schema-design|align-api-schema-design]]) — การรวมไว้ในไฟล์เดียวกัน**ไม่ได้แปลว่าทั้งสองเป็นระบบเดียวกัน**
> เก็บต้นฉบับไว้เพื่อ traceability เท่านั้น ถ้าจะยึดเป็น source of truth ให้ใช้ตารางที่ 2 (sync แล้ว) เสมอ

## ระบบของฉันคืออะไร

ระบบเก็บ **เนื้อหาและวิธีการสอน** ที่อาจารย์ผู้สอนในหลักสูตรสร้างขึ้น
อาจารย์แต่ละคนเลือก **เล่มหลักสูตรตามปี พ.ศ.** ที่สอน แล้วเติม
**ผลลัพธ์การเรียนรู้ (CLO)** และ **เนื้อหา/กิจกรรมการสอนจริงแต่ละสัปดาห์**
มีสถานะ **รอพิจารณา → รับทราบ** โดย **ประธานหลักสูตร** เป็นคนกดเปลี่ยนสถานะ

## ตารางเทียบโครงสร้าง (1) — ต้นฉบับจากโปรเจกต์การบ้าน `align-homework-w6`

| | 🔧 LeaveEasy | 👤 ระบบของฉัน (align-homework-w6) |
|---|---|---|
| 📁 โฟลเดอร์หลัก | `leaveRequests` | `teachingPlans` |
| 📁 โฟลเดอร์ประเภท | `leaveTypes` | `curriculumVersions` |
| 📁 โฟลเดอร์ย่อย | `approvals` | `weeklyContents` |
| ✏️ ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `instructorId` · `instructorName` |
| 🔀 สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | รอพิจารณา · รับทราบ |
| 👤 คนที่สร้างรายการ | พนักงาน | อาจารย์ผู้สอน |
| 👤 คนที่เปลี่ยนสถานะ | หัวหน้า | ประธานหลักสูตร |
| 📝 ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `activityDescription` |
| 🤖 งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาให้อัตโนมัติ | จับคู่/ตรวจสอบเนื้อหารายสัปดาห์ให้สอดคล้องกับ CLO |

## ตารางเทียบโครงสร้าง (2) — Sync กับ schema จริงของ ALIGN (`nmc-align-2026`)

> คอลัมน์ "ระบบของฉัน" อ้างอิงชื่อ collection/field จาก
> [[docs/02-design/02-technical/align-api-schema-design|align-api-schema-design]] §2.2 (`teaching_records`, `curricula`) และ §3.7 (`teaching_record`)
> แทนชื่อ camelCase ของตารางที่ (1) ด้านบน

| | 🔧 LeaveEasy | 👤 ระบบของฉัน (ALIGN จริง) |
|---|---|---|
| 📁 โฟลเดอร์หลัก | `leaveRequests` | `teaching_records` (top-level collection, auto id) |
| 📁 โฟลเดอร์ประเภท | `leaveTypes` | `curricula` (doc id = `year_code` เช่น `"2565"`/`"2570"`) |
| 📁 โฟลเดอร์ย่อย | `approvals` | ไม่มี subcollection แยก — `week_no` + `topic` เป็น field ตรงบน `teaching_record` เอง (ดูหมายเหตุข้อ 1 ด้านล่าง) |
| ✏️ ช่องบอกว่าเป็นของใคร | `requesterId` · `requesterName` | `created_by` (Firebase Auth UID เท่านั้น — ไม่มี field ชื่อสำเร็จรูปเก็บซ้ำ ต้อง lookup จาก `users/{uid}` ถ้าจะแสดงชื่อ) |
| 🔀 สถานะทั้งหมด | รอพิจารณา · อนุมัติ · ไม่อนุมัติ | `status`: `draft_ai_pending` · `confirmed` (ดูหมายเหตุข้อ 2 — **ไม่ตรงกับ workflow นี้**) |
| 👤 คนที่สร้างรายการ | พนักงาน | อาจารย์ผู้สอน (`created_by` ต้องเท่ากับ `course.instructor_id`) |
| 👤 คนที่เปลี่ยนสถานะ | หัวหน้า | ในระบบจริงคือ**อาจารย์ผู้สอนคนเดิม**กดยืนยันเอง ไม่ใช่ประธานหลักสูตร (ดูหมายเหตุข้อ 2) |
| 📝 ช่องข้อความยาวที่ AI จะอ่าน | `reason` | `topic` (§3.7 — "หัวข้อการสอนจริง ใช้เป็น input เทียบกับ `syllabus.content`") — ตอบคำถามเดิมเรื่องชื่อ `activityDescription` แล้ว (ดูหมายเหตุข้อ 3) |
| 🤖 งานที่ AI ช่วย (สัปดาห์ที่ 8) | จัดประเภทการลาให้อัตโนมัติ | จับคู่/ตรวจสอบเนื้อหารายสัปดาห์ให้สอดคล้องกับ CLO (`ai_match_result`, §3.9) |

## หมายเหตุ / สิ่งที่ต้องยืนยันกับอาจารย์

1. **ไม่มี `weeklyContents` แยกในระบบจริง** — schema จริงเก็บ `week_no`/`topic` เป็น field ตรงบน `teaching_record` แต่ละ document (1 บันทึกการสอน = 1 สัปดาห์) ไม่ได้ nest เป็น subcollection ประวัติการรับทราบ/ตีกลับ — ถ้าการบ้านต้องการ subcollection จริงๆ (เช่น เก็บประวัติแก้ไขหลายรอบต่อสัปดาห์) เป็นการออกแบบที่ต่างจาก ALIGN จริง ต้องตัดสินใจแยก *(ตอบคำถามเดิมของตาราง (1) ข้อ "โฟลเดอร์ย่อย weeklyContents ถูกต้องไหม")*
2. **สถานะ "รอพิจารณา → รับทราบ โดยประธานหลักสูตร" ไม่ตรงกับกฎจริงของ ALIGN** — กฎ #3 ของระบบจริงระบุว่า AI เป็นค่าตั้งต้นเท่านั้น และ**อาจารย์ผู้สอนเจ้าของวิชานั้นเองเป็นคนเดียว**ที่ยืนยันผล (`draft_ai_pending → confirmed`) ไม่มีขั้นตอนให้ประธานหลักสูตร/ผู้บริหารหลักสูตรมากดอนุมัติบันทึกการสอนเลย — ถ้าจะให้ตรงกับ ALIGN ต้องเปลี่ยนเป็น "อาจารย์ผู้สอนยืนยันเอง" ไม่ใช่คงไว้เป็นสมมติฐานเดิม — เรื่องเดียวกันเคยพบอีกครั้งตอนออกแบบฟอร์ม "ยื่นคำร้องรายวิชาใหม่" ใน `align-homework-w6` (สถานะ `รอพิจารณา`) เทียบกับ entity `course` จริงของ ALIGN (`align-api-schema-design.md` §3.5) ที่**ไม่มี field สถานะ/ไม่มี workflow อนุมัติเลย** — สร้างรายวิชาโดยผู้บริหารหลักสูตรโดยตรง
3. **`activityDescription` ถูกตอบแล้ว** — map ตรงกับ field `topic` ของ `teaching_record` จริง *(ตอบคำถามเดิมของตาราง (1) ข้อ "ชื่อ field activityDescription เหมาะสมหรือควรเปลี่ยนชื่อ")*
4. งาน AI สัปดาห์ที่ 8 ตรงกับที่อาจารย์กำหนดหัวข้อไว้หรือไม่ (ยังไม่ได้ตรวจ — เปิดค้างอยู่ทั้งสองฝั่ง)
