# Prototype: หน้าจอแอป ALIGN — บทบาทอาจารย์ผู้สอน (Instructor)

> **เวอร์ชันนี้แทนที่เวอร์ชันแรก** ที่อ้างอิงจาก Canva pitch deck เท่านั้น (4 หน้าจอ, ก่อนมี `DESIGN.md`) — ต้นฉบับเดิมเก็บไว้ที่ [[../../00-archived/align-app-screens-pitch-deck-v1|00-archived/align-app-screens-pitch-deck-v1]] เพื่อรักษาประวัติ แนวคิด flow หลักจากเวอร์ชันเดิม (ป้อนข้อมูลวิชา → บันทึกการสอน → AI ประมวลผล → เห็นความสอดคล้อง) ยังคงอยู่และถูกขยายในเอกสารนี้ให้ครบ Epic E1–E5 ตาม [[../../01-requirements/02-plan/product-backlog|product-backlog]] (AB-01 ถึง AB-23)

อ้างอิงคู่กับ:
- [[../../../DESIGN.md|DESIGN.md]] — โทเค็นสี/ตัวอักษร/สเปซ, component, และ UX Rules ทั้งหมดที่ใช้ในเอกสารนี้
- [[../../01-requirements/01-spec/requirement-align|requirement-align]] — สเปคและ Root Cause
- [[../../01-requirements/02-plan/product-backlog|product-backlog]] — User Story/AC ที่แต่ละหน้าจอ map ถึง
- [[../02-technical/align-technical-design|align-technical-design]] — entity/state (`ai_match_result`, `syllabus_gap_result`, `clo_coverage_summary`) ที่หน้าจอเหล่านี้แสดงผล

เอกสารคู่กัน: [[align-program-admin-screens|align-program-admin-screens]] (บทบาทผู้บริหารหลักสูตร), [[align-navigation-flow|align-navigation-flow]] (แผนภาพลำดับการไปมาระหว่างหน้าจอทั้ง 2 บทบาท), [[align-user-journey|align-user-journey]] (User Journey Map)

---

## 0. โครงหน้าจอร่วม (App Shell) — ใช้ทั้งบทบาทอาจารย์และผู้บริหารหลักสูตร

ทุกหน้าจอในเอกสารนี้และใน [[align-program-admin-screens|align-program-admin-screens]] อยู่ภายใต้โครงหน้าจอเดียวกัน:

- **App Header** (สูงคงที่, พื้นหลัง `bg.surface`, เส้นขอบล่าง `border.default`, padding `space.md`):
  - ซ้าย: **ALIGN wordmark** — ข้อความ "ALIGN" น้ำหนัก SemiBold ขนาด `text.h2` สี `primary.green` (ตาม DESIGN.md §1.4) **ไม่มีไอคอนประกอบ** ในต้นแบบนี้
  - ขวา: ชื่อผู้ใช้ + badge บทบาทปัจจุบัน (Instructor/Program Administrator) + เมนู account แบบ dropdown เรียบ (ไม่มี avatar สีสันจัด)
  - > **หมายเหตุเปิด (Open Question)**: `DESIGN.md` §1.4 ยังไม่มีมติเรื่องไอคอน/สัญลักษณ์เฉพาะของ ALIGN (มีแค่ข้อเสนอ "เส้นโค้ง 2 เส้นไขว้กัน" ที่ยังไม่ยืนยัน) ต้นแบบนี้จึงใช้ **wordmark ข้อความล้วนชั่วคราว** ตามแนวทางที่ DESIGN.md ระบุไว้เป็นทางเลือกระยะแรก หากทีมตัดสินใจไอคอนภายหลัง ต้องกลับมาอัปเดต App Header ในทุกหน้าจอของเอกสารนี้และเอกสารบทบาทผู้บริหารหลักสูตร
- **Side Navigation** (ซ้าย, กว้างคงที่, พื้นหลัง `bg.base`, ไอคอน line icon stroke 1.5px ตาม DESIGN.md §2.5): เมนูเปลี่ยนตามบทบาทของผู้ใช้ที่ login (ดูเมนูของแต่ละบทบาทในหัวข้อถัดไปและใน [[align-program-admin-screens|align-program-admin-screens]]) — ระบบมี 2 บทบาทเท่านั้น (`CLAUDE.md`), ไม่มีการสลับบทบาทเองในแอป (สิทธิ์ผูกกับ account)
- **Content Area**: พื้นหลัง `bg.base`, การ์ด/panel ใช้ `bg.surface` + `radius.md` + `shadow.card` เบาๆ ตาม DESIGN.md §2.4, เว้นขอบ content จาก header/side nav ด้วย `space.xl`
- **Curriculum tag ทุกจุดที่มี CLO/PLO/รายวิชา** ใช้ Curriculum Tag / Chip token `curriculum.2565` (taupe) หรือ `curriculum.2570` (moss) เสมอ ไม่มีข้อยกเว้น (UX Rule 2)

เมนู Side Navigation ของบทบาท **อาจารย์ผู้สอน**:

| ไอคอน (แนวคิด) | label | ลิงก์ไปหน้าจอ |
|---|---|---|
| home | หน้าแรก | หน้าจอ 1 — แดชบอร์ด |
| book | รายวิชาของฉัน | หน้าจอ 2 — รายวิชาของฉัน |
| grid | แผนที่ CLO × สัปดาห์ | หน้าจอ 7 (เลือกวิชาก่อนเข้า) |

หมายเหตุ: การ "บันทึกการสอน" (หน้าจอ 5) และ "สรุปรายวิชา" (หน้าจอ 8) ไม่มีปุ่มตายตัวใน Side Navigation เพราะเป็น context เฉพาะรายวิชา — เข้าถึงจากภายในหน้า "รายวิชาของฉัน" หรือปุ่ม CTA บนแดชบอร์ด (ดู [[align-navigation-flow|align-navigation-flow]])

---

## หน้าจอ 1 — หน้าแรก / แดชบอร์ด (Dashboard)

**Map**: AB-11, AB-12, AB-13 (ทางเข้า), AB-23

**จุดประสงค์**: ให้อาจารย์เห็นภาพรวมความสอดคล้องของทุกวิชาที่สอนทันทีที่เข้าระบบ โดยไม่ต้องเปิดดูทีละวิชา

**Layout** (บนลงล่าง, เว้น `space.xl` ระหว่าง section ตามหลัก "whitespace มาก่อนความหนาแน่น"):

1. **แถว Stat Tile** (AB-11): Stat Tile แสดงตัวเลข `text.display` ของ % ความสอดคล้องรวม — คำนวณเฉพาะจากค่า `coverage_percent` ที่มาจาก `ai_match_result` ที่ `state = confirmed` เท่านั้น (ตามสูตร AB-20/AB-21) พร้อม label กำกับชัดเจนว่า **"ยืนยันแล้วเท่านั้น"** ใต้ตัวเลข (text.caption, สี `text.secondary`) — ห้ามผสมค่า draft เข้ามาคำนวณค่านี้เด็ดขาด (UX Rule 1)
2. **รายชื่อรายวิชาที่สอน** (AB-11): ใช้ Course/Row List Item ต่อแถว — แต่ละแถวมี: ชื่อวิชา, Curriculum Tag (`curriculum.2565`/`curriculum.2570`), Match % Indicator แบบย่อ (bar สั้น, เส้นทึบสี `status.confirmed` เพราะเป็นค่าระดับวิชาที่คำนวณจาก confirmed แล้ว), สถานะครบถ้วน CLO–PLO (badge เล็ก) — กดแถวเพื่อไปหน้าจอ 8 (สรุปรายวิชา)
3. **Gap Alert Banner** (AB-12): แจ้งเตือน CLO ที่ยังไม่มีข้อมูลการสอน/หลักฐานรองรับ ต่อรายวิชา — โทน `status.gap` + `status.gap-tint` เป็นพื้นหลัง แสดงเป็น banner inline (ไม่ใช่ modal บังคับปิด ตาม UX Rule 6) ข้อความเป็นกลาง เช่น "CLO 4 (รายวิชา XXX · หลักสูตร 2570) — ยังไม่มีข้อมูลการสอนรองรับ" พร้อมปุ่มลิงก์ "บันทึกการสอน" ไปหน้าจอ 5 โดยตรง — ระบุ Curriculum Tag ของวิชานั้นกำกับเสมอ
4. **ส่วนเปรียบเทียบ "การสอนจริง vs CLO/course syllabus"** (AB-23) — **แยกเป็น section อิสระ ไม่ปนกับ Gap Alert Banner ของข้อ 3** (ตาม AC ของ AB-23 ที่ระบุชัดว่าต้องแยกจาก AB-11/AB-12): การ์ดต่อรายวิชา แสดงจำนวนหัวข้อที่ขาด (`missing_topics`) และหัวข้อที่สอนเพิ่มนอกแผน (`extra_topics`) จาก `syllabus_gap_result` ที่ `state = confirmed` เท่านั้น — ถ้ายังไม่มีผลที่ยืนยันแล้ว ให้ใช้ Empty State ข้อความ "ยังไม่มีผลวิเคราะห์ที่ยืนยันแล้วสำหรับวิชานี้" (ไม่ใช้คำว่า "ไม่มีข้อมูล/error") พร้อมปุ่มลิงก์ไปหน้าจอ 8

**Curriculum tag**: ทุกแถววิชา/ทุก banner ต้องมี tag กำกับ (UX Rule 2)

**States**: ปกติ (มีข้อมูลครบ), Empty State (อาจารย์ยังไม่มีวิชาที่ตั้งค่าเสร็จ — ข้อความชวนไปหน้าจอ 2 "เริ่มตั้งค่ารายวิชาแรกของคุณ"), มี Gap Alert vs ไม่มี Gap Alert (ซ่อน banner section 3 ทั้งหมดถ้าไม่มี gap ใดๆ แทนที่จะโชว์ "ไม่มี gap" แบบเขียว เพื่อลดความหนาแน่นตาม UX Rule 5)

**จุดเชื่อมต่อ**: เป็นหน้า landing หลังเข้าสู่ระบบ → ไปหน้าจอ 2 (รายวิชาของฉัน), หน้าจอ 5 (บันทึกการสอน จาก CTA แจ้งเตือน), หน้าจอ 7 (แผนที่ CLO×สัปดาห์ ผ่านเมนู), หน้าจอ 8 (สรุปรายวิชา จากแถวรายวิชา) — รายละเอียดเต็มดู [[align-navigation-flow|align-navigation-flow]]

---

## หน้าจอ 2 — รายวิชาของฉัน (My Courses List)

**Map**: AB-02 (ทางเข้า), AB-19 (ทางเข้า)

**จุดประสงค์**: จุดรวมรายวิชาทั้งหมดที่อาจารย์รับผิดชอบ เป็นทางเข้าไปตั้งค่า CLO/PLO และ course syllabus ก่อนเริ่มบันทึกการสอนได้ (กฎทางธุรกิจ #1)

**Layout**:
- หัวข้อหน้า (`text.h1`) "รายวิชาของฉัน" + ปุ่ม "เพิ่มรายวิชา" (primary button, สี `primary.green`)
- รายการ Course/Row List Item ต่อวิชา แสดง: รหัส/ชื่อวิชา, Curriculum Tag, สถานะความครบถ้วน CLO–PLO เป็น badge สองแบบ:
  - "พร้อมบันทึกการสอน" — เส้นขอบทึบ `status.confirmed`
  - "ยังตั้งค่าไม่ครบ" — เส้นขอบประ `status.draft` (สื่อว่ายังไม่ผ่านเงื่อนไข ไม่ใช่ error)
- แต่ละแถวกดแล้วเข้าหน้าจอ 3/4 (จัดการรายวิชา)

**Curriculum tag**: บังคับทุกแถว (UX Rule 2)

**States**: Empty State เมื่อยังไม่มีวิชาเลย — ข้อความ "ยังไม่มีรายวิชา — เริ่มเพิ่มรายวิชาแรกของคุณ" พร้อมปุ่ม CTA (ไม่ใช้โทนลบ)

**จุดเชื่อมต่อ**: มาจากหน้าจอ 1 (เมนู "รายวิชาของฉัน") → ไปหน้าจอ 3 (จัดการรายวิชา: CLO & ผูก PLO)

---

## หน้าจอ 3 — จัดการรายวิชา: CLO และผูก PLO

**Map**: AB-02, AB-03

**จุดประสงค์**: ให้อาจารย์ป้อน CLO ของรายวิชา (ระบุกลุ่มหลักสูตรของวิชาก่อนเสมอ) และผูกความสัมพันธ์ CLO–PLO ภายในกลุ่มหลักสูตรเดียวกันเท่านั้น ก่อนระบบจะปลดล็อกให้บันทึกการสอนได้

**Layout**: หน้าจัดการรายวิชาใช้ tab 2 แท็บ — แท็บนี้คือ "CLO & PLO" (แท็บที่สองคือ "Course Syllabus" หน้าจอ 4)

1. หัวหน้า: ชื่อวิชา + Curriculum Tag ของวิชานี้ (fixed ตอนสร้างวิชา, ไม่ให้แก้ทีหลังในหน้านี้เพื่อกันข้อมูลปนกลุ่ม — ตาม AB-02: "ไม่สามารถป้อน CLO โดยไม่ระบุกลุ่มหลักสูตรได้")
2. รายการ CLO ของวิชา (การ์ดต่อข้อ): รหัส CLO (font `IBM Plex Mono` ตาม DESIGN.md §2.2), คำอธิบาย, และรายการ PLO ที่ผูกไว้แสดงเป็น chip เล็ก — **ตัวเลือก PLO ที่เลือกผูกได้ต้องกรองมาจากกลุ่มหลักสูตรเดียวกับ CLO นี้เท่านั้น** (dropdown/selector ไม่แสดง PLO ต่างกลุ่มให้เลือกเลย ป้องกันการผูกผิดตั้งแต่ UI ไม่ใช่แค่ validate ตอนบันทึก — ตรงกับ constraint ที่ backend บังคับใน `clo_plo_mapping`)
3. Gap Alert Banner แบบ inline ท้ายรายการ: ถ้ายังไม่มี CLO ใดผูกกับ PLO เลยสักข้อ แสดงข้อความ "ยังไม่มี CLO ที่ผูกกับ PLO — ต้องผูกอย่างน้อย 1 คู่ก่อนจึงบันทึกการสอนได้" (โทน `status.gap`, เป็นกลาง)
4. ปุ่ม "เพิ่ม CLO" ท้ายรายการ

**Curriculum tag**: แสดงที่หัวหน้าและกำกับตัวเลือก PLO ทุกจุด (UX Rule 2)

**States**: Empty State (ยังไม่มี CLO เลย), บล็อกไม่ให้ไปหน้าจอ 5 (บันทึกการสอน) จนกว่าเงื่อนไข "CLO ≥1 ผูก PLO ≥1" ผ่าน — สะท้อนด้วย badge สถานะที่หน้าจอ 2

**จุดเชื่อมต่อ**: จากหน้าจอ 2 → สลับแท็บไปหน้าจอ 4 (Course Syllabus) ได้ในหน้าเดียวกัน → เมื่อเงื่อนไขผ่านแล้ว ปลดล็อกปุ่ม "บันทึกการสอน" ที่หน้าจอ 2 ให้ไปหน้าจอ 5

---

## หน้าจอ 4 — จัดการรายวิชา: Course Syllabus

**Map**: AB-19

**จุดประสงค์**: ป้อน/อัปโหลด course syllabus ของรายวิชาเป็นข้อมูลอ้างอิงแยกจาก CLO/PLO แต่ผูกกับวิชาเดียวกัน ใช้เป็น input ให้ AI วิเคราะห์ gap ภายหลัง (หน้าจอ 6, 8)

**Layout** (แท็บที่สองของหน้าจัดการรายวิชา ต่อจากหน้าจอ 3):

1. ตัวเลือก 2 วิธีป้อนข้อมูล (ไม่ใช่ mutually exclusive): "ป้อนหัวข้อเอง" (ฟอร์มรายการ สัปดาห์ที่ + หัวข้อ + รายละเอียด) หรือ "อัปโหลดไฟล์ syllabus" (upload widget เรียบ ไม่มีสีสันจัด)
2. ตารางรายการหัวข้อ syllabus ปัจจุบัน แสดงคอลัมน์: สัปดาห์ที่, หัวข้อ, รายละเอียด (ย่อ), ปุ่มแก้ไข/ลบต่อแถว
3. แถบท้ายหน้า: "แก้ไขล่าสุดโดย [ชื่อ] เมื่อ [วันที่]" (`text.caption`, สี `text.secondary`) — สอดคล้องกับ `updated_by`/`updated_at`
4. Curriculum Tag ของวิชานี้แสดงกำกับที่หัวหน้า (สืบทอดจาก `course.curriculum_id`)

**Curriculum tag**: กำกับที่หัวหน้าเช่นเดียวกับหน้าจอ 3 (UX Rule 2)

**States**: Empty State เมื่อยังไม่มี syllabus — ข้อความ "ยังไม่มีข้อมูล course syllabus ของวิชานี้ — เริ่มป้อนหรืออัปโหลด" (ไม่ใช้คำว่า "ขาด/ผิดพลาด")

**จุดเชื่อมต่อ**: แท็บคู่กับหน้าจอ 3 ภายในหน้าจัดการรายวิชาเดียวกัน — syllabus ที่ป้อนที่นี่ถูกใช้เป็น input ให้ AI Review Panel (หน้าจอ 6) และสรุปรายวิชา (หน้าจอ 8)

---

## หน้าจอ 5 — บันทึกการสอน (Teaching Record Entry)

**Map**: AB-05, AB-06, AB-07 (พฤติกรรมของ Evidence Attachment List)

**จุดประสงค์**: บันทึกหัวข้อการสอนที่เกิดขึ้นจริงหลังสอนแต่ละครั้ง พร้อมแนบหลักฐาน แทนการติ๊กด้วยมือ — **เข้าถึงหน้านี้ได้เฉพาะรายวิชาที่ `clo_plo_ready = true` เท่านั้น** (กฎทางธุรกิจ #1) ถ้ายังไม่ครบเงื่อนไข ปุ่ม "บันทึกการสอน" ที่หน้าจอ 2 จะถูก disable (สี `text.disabled`) พร้อม tooltip อธิบายเหตุผลแทนการปล่อยให้กดแล้วเจอ error

**Layout**:
1. หัวหน้า: ชื่อวิชา + Curriculum Tag + สัปดาห์ที่สอน (dropdown/เลขสัปดาห์)
2. ช่องหัวข้อการสอน (text field, `text.body`) + วันที่สอนจริง (date picker, ค่าเริ่มต้น = วันนี้ ไม่ใช่วันที่ในอนาคต — สื่อว่าเป็นการบันทึกหลังสอนจริง ไม่ใช่แผนล่วงหน้า)
3. **Evidence Attachment List**: รายการไฟล์แนบ รองรับแนบได้มากกว่า 1 ไฟล์ (AB-06) — แต่ละแถวมีปุ่ม "ลบ" ก่อนกดยืนยัน, แสดงชื่อไฟล์ทั้งหมดให้ตรวจสอบก่อนบันทึกจริง — **ต้องเช็คสิทธิ์ก่อนแสดง thumbnail เสมอ** (AB-07/PDPA, UX Rule 4): ถ้าไฟล์มี flag `contains_student_pii` และผู้ดูไม่มีสิทธิ์ ให้ซ่อน thumbnail/preview ทั้งหมด แสดงเพียงไอคอนไฟล์ทั่วไป + ชื่อไฟล์เท่านั้น
4. ปุ่ม "บันทึกและให้ AI ประมวลผล" (primary, `primary.green`) — กดแล้วไปหน้าจอ 6 ทันที (ไม่ auto-save เป็นข้อมูลจริงจนกว่าจะยืนยันผล AI ในหน้าจอ 6)

**Curriculum tag**: กำกับที่หัวหน้า (UX Rule 2)

**States**: ฟอร์มว่าง (เริ่มต้น), มีไฟล์แนบแล้วอย่างน้อย 1 ไฟล์ (ปุ่มบันทึก enable), Empty State ของ Evidence Attachment List ก่อนแนบไฟล์แรก ("ยังไม่มีไฟล์แนบ — ลากไฟล์มาวางหรือเลือกไฟล์")

**จุดเชื่อมต่อ**: มาจากหน้าจอ 2/1 → ไปหน้าจอ 6 (AI Review Panel) ทันทีหลังกดบันทึก

---

## หน้าจอ 6 — ตรวจสอบและยืนยันผล AI (AI Review Panel)

**Map**: AB-08, AB-09, AB-10, AB-20, AB-21, AB-22

**จุดประสงค์**: หน้านี้คือหัวใจของกฎ human-in-the-loop (กฎทางธุรกิจ #3) — แสดงผลจาก AI ทั้งหมด (ผลจับคู่ CLO/PLO, สัดส่วน %, ความถี่ที่แมทช์, ผล gap เทียบ syllabus) เป็น **draft ล้วน** ให้อาจารย์ตรวจสอบ แก้ไข และกด "ยืนยัน" ก่อนเท่านั้นจึงจะถือเป็นข้อมูลจริง

**Layout** (แบ่ง 2 ส่วนชัดเจนตามที่ technical design แยก entity คนละตัว — **ห้ามปนกันในสายตาเดียว**):

### ส่วนที่ 1 — ผลจับคู่ CLO/PLO (จาก `ai_match_result`, AB-08/AB-09/AB-10)

- ใช้ **AI Review Panel** component ตาม DESIGN.md §3: ปุ่มหลักคือ **"ยืนยัน"** ไม่ใช่ auto-save, มีปุ่ม **"แก้ไข"** และ **"ปฏิเสธ"** แยกชัดเจนต่อรายการ
- ต่อ CLO แต่ละข้อที่ AI จับคู่ได้ (เฉพาะ CLO ในกลุ่มหลักสูตรเดียวกับวิชานี้เท่านั้น — AB-08): แสดง **Match % Indicator แบบ Draft** — เส้นขอบ**ประ** สี `status.draft`, พื้นหลัง `status.draft-tint` พร้อม label "ผลจาก AI — ยังไม่ยืนยัน" กำกับเสมอ (แยกจาก Confirmed อย่างชัดเจนที่สุดตาม UX Rule 1 — นี่คือกฎที่พลาดไม่ได้ที่สุดของระบบ)
- รายการ PLO ที่เชื่อมโยงต่อจาก CLO นั้น (AB-09) แสดงเป็น chip ใต้ CLO แต่ละข้อ พร้อมข้อความสรุป เช่น "PLO 2 · PLO 4 เชื่อมแล้ว (หลักสูตร 2570)" — ระบุ Curriculum Tag กำกับ (สืบทอดจากกลุ่มหลักสูตรเดียวกับ CLO)
- อาจารย์แก้ไขได้ก่อนยืนยัน (AB-10): ปุ่ม "แก้ไข" เปิดให้ปรับค่า % หรือปลด CLO ที่จับคู่ผิดออก (state เปลี่ยนเป็น "edited" — ยังคงแสดงเป็นเส้นประ `status.draft` จนกว่าจะกด "ยืนยัน" จริง)
- ปุ่ม "ยืนยันผลจับคู่ทั้งหมด" ต่อท้าย — กดแล้ว badge ของรายการที่ยืนยันเปลี่ยนเป็น **เส้นทึบสี `status.confirmed`** ทันที (visual feedback ชัดเจนว่าเปลี่ยนสถานะแล้ว)

### ส่วนที่ 2 — ผลวิเคราะห์ Gap เทียบ Course Syllabus (จาก `syllabus_gap_result`, AB-22 — **แยกจากส่วนที่ 1 โดยสิ้นเชิงตามสเปค**)

- Gap Alert Banner โทน `status.gap`/`status.gap-tint` แสดง 2 รายการแยกกัน:
  - **หัวข้อที่ขาด** (`missing_topics`) — หัวข้อใน syllabus ที่ยังไม่พบการสอนจริงรองรับ
  - **หัวข้อที่สอนเพิ่มนอกแผน** (`extra_topics`) — หัวข้อที่สอนจริงแต่ไม่มีใน syllabus
- ทั้งสองรายการมีเส้นขอบ**ประ** สี `status.draft` เช่นเดียวกับส่วนที่ 1 จนกว่าจะยืนยัน (state เดียวกัน: draft/edited/confirmed/rejected) — อาจารย์แก้ไขรายการก่อนกดยืนยันได้เช่นกัน
- ปุ่ม "ยืนยันผลวิเคราะห์ gap" แยกต่างหากจากปุ่มยืนยันผลจับคู่ CLO/PLO ในส่วนที่ 1 (เพราะเป็นคนละ endpoint/entity — บันทึกอิสระจากกัน อาจยืนยันส่วนใดส่วนหนึ่งก่อนก็ได้)
- โทนข้อความเป็นกลาง ไม่ตัดสิน เช่น "พบหัวข้อใน syllabus ที่ยังไม่มีบันทึกการสอนรองรับ 2 หัวข้อ" (ไม่ใช้ "ผิดพลาด/สอนไม่ครบ")

**Curriculum tag**: กำกับทุกจุดที่มี CLO/PLO (UX Rule 2)

**States**: กำลังประมวลผล (AI กำลังทำงาน — ใช้ inline loading indicator เรียบ ไม่ใช้แอนิเมชันดึงความสนใจ), draft ครบทุกรายการยังไม่ยืนยัน, ยืนยันบางส่วนแล้ว (ผสม draft+confirmed ในหน้าเดียวได้ แต่ต้องแยก visual ชัดต่อรายการเสมอ), ปฏิเสธผลบางรายการ (state='rejected' — เอาออกจากรายการที่จะถูกนับ ไม่ลบประวัติ)

**จุดเชื่อมต่อ**: มาจากหน้าจอ 5 ทันทีหลังบันทึก → เมื่อยืนยันแล้วกลับไปหน้าจอ 1 (แดชบอร์ด, ค่าจะอัปเดต) หรือหน้าจอ 8 (สรุปรายวิชา)

---

## หน้าจอ 7 — แผนที่ CLO × สัปดาห์ (CLO × Week Map)

**Map**: AB-13

**จุดประสงค์**: ให้เห็นภาพรวมความคืบหน้าตลอดภาคการศึกษาต่อ CLO แต่ละข้อ

**Layout**: ใช้ **CLO × Week Grid** component ตาม DESIGN.md §3 — ตารางความหนาแน่นสูง ใช้ `space.sm` ภายในเซลล์แต่เว้น `space.lg` รอบตารางทั้งหมด (ไม่ให้อึดอัดตามหลัก Muji)

1. แถวหัวตาราง: สัปดาห์ที่ 1–16 (หรือตามจำนวนสัปดาห์จริงของเทอม)
2. คอลัมน์ซ้าย: CLO แต่ละข้อ (รหัสด้วย `IBM Plex Mono`) พร้อม Curriculum Tag กำกับต่อแถว
3. แต่ละเซลล์: จำนวนชิ้นงานสะสม (ตัวเลขเล็ก) — เซลล์ที่มีชิ้นงาน ≥1 เน้นพื้นหลังอ่อน `primary.green-tint`, เซลล์ว่างเป็น `bg.surface-sunken` เฉยๆ (ไม่ใช้สีแดง/เตือนแบบเดิม เพราะช่องว่างยังไม่ถือเป็น gap จนกว่าจะจบเทอม — เชื่อมโยงกับ Gap Alert ที่หน้าจอ 1 ต่างหาก)
4. การ์ดสรุปด้านบนตาราง: จำนวนชิ้นงานสะสมรวมทั้งวิชา (Stat Tile ขนาดเล็กกว่าแดชบอร์ด)
5. แถวท้ายตาราง/ด้านข้าง: "สถานะเชื่อมโยงถึง PLO" ต่อ CLO — ใช้ Curriculum Tag + chip PLO เดียวกับหน้าจอ 6 แต่แสดงเฉพาะที่ **confirmed แล้ว** เท่านั้น (ไม่แสดง draft ในหน้าสรุปนี้ เพื่อไม่ให้ปนกับข้อมูลที่ยังไม่ยืนยัน)

**Curriculum tag**: กำกับทุกแถว CLO (UX Rule 2)

**States**: Empty State ทั้งตาราง (ยังไม่มีบันทึกการสอนเลย), ตารางบางส่วน (ปกติระหว่างเทอม)

**จุดเชื่อมต่อ**: เข้าจากเมนู Side Navigation "แผนที่ CLO × สัปดาห์" (เลือกวิชาก่อน) หรือจากหน้าจอ 8 (สรุปรายวิชา)

---

## หน้าจอ 8 — สรุปรายวิชา (Course Summary)

**Map**: AB-11 (ระดับวิชา), AB-16 (เนื้อหา Area of Improvement), AB-20, AB-21, AB-22/23 (สรุป gap ระดับวิชา)

**จุดประสงค์**: สรุปความสอดคล้อง CLO/PLO, แนวโน้ม, และจุดที่ควรพัฒนาของรายวิชาหนึ่งวิชา ก่อนออกเอกสาร

**Layout**:
1. หัวหน้า: ชื่อวิชา + Curriculum Tag + Stat Tile ตัวเลข % ความสอดคล้องรวมของวิชานี้ (เส้นทึบ `status.confirmed` เท่านั้น เพราะเป็นค่าที่คำนวณจาก confirmed แล้วเสมอตามนิยาม `clo_coverage_summary`)
2. รายการ CLO แต่ละข้อ: Match % Indicator (เส้นทึบ confirmed) + **ความถี่ที่แมทช์ (match frequency)** แสดงประกอบกันเสมอในบรรทัดเดียว เช่น "แมทช์แล้ว 65% · พบหลักฐานรองรับ 4 ครั้ง" (AB-21 — ห้ามแสดงแค่สัดส่วนอย่างเดียวโดยไม่มีความถี่)
3. **Trends** (แนวโน้มตลอดเทอม): กราฟเส้นเรียบง่าย (line, สีเดียว `primary.green`, ไม่มี gradient) แสดงแนวโน้ม coverage_percent ตามช่วงเวลา
4. **สรุป Gap เทียบ course syllabus** (จาก `syllabus_gap_result` ที่ confirmed แล้ว): รายการ missing/extra topics แบบย่อ (ลิงก์ไปหน้าจอ 6 หากมี draft ค้างอยู่ที่ยังไม่ยืนยัน)
5. **Area of Improvement** (AB-16): แบ่ง 3 ระดับตามสเปค — เนื้อหาอ้างอิงเฉพาะช่องว่าง/CLO ขาดหลักฐาน, match % ต่ำ, หรือ gap เทียบ syllabus ที่ตรวจพบจริงและ**ยืนยันแล้ว**เท่านั้น (กฎทางธุรกิจ #3) ไม่ใช่ข้อความลอยๆ — ใช้โทนภาษาเป็นกลาง ให้กำลังใจ (UX Rule 3) เช่น "CLO 4 ยังมีหลักฐานรองรับน้อย (ความถี่ 1 ครั้ง) — พิจารณาเพิ่มกิจกรรม/ชิ้นงานที่เชื่อมโยงกับหัวข้อนี้ในรอบถัดไป"
6. ปุ่ม "ออกเอกสาร Word" ท้ายหน้า → เปิดหน้าจอ 9

**Curriculum tag**: กำกับที่หัวหน้าและทุกจุดที่มี CLO/PLO (UX Rule 2)

**States**: ปกติ (มีข้อมูลยืนยันครบ), บางส่วนยังไม่ยืนยัน (แสดง banner เตือนกลางๆ ว่า "มีผลจาก AI ที่ยังไม่ยืนยัน N รายการ" พร้อมลิงก์กลับไปหน้าจอ 6 — ไม่ปิดกั้นการดูหน้าสรุป แต่กันไม่ให้ข้อมูล draft ปนเข้าไปในตัวเลขสรุปและเอกสาร export)

**จุดเชื่อมต่อ**: มาจากหน้าจอ 1 (แถวรายวิชา) หรือหน้าจอ 7 → ไปหน้าจอ 9 (Word Export Panel) หรือหน้าจอ 6 (ถ้ามี draft ค้าง)

---

## หน้าจอ 9 — ออกเอกสาร Word (Word Export Panel)

**Map**: AB-15, AB-16, AB-18

**จุดประสงค์**: สร้างเอกสารสรุปความสอดคล้อง CLO/PLO เป็นหลักฐานประกันคุณภาพ/มคอ. โดยอ้างอิงเฉพาะข้อมูลที่ยืนยันแล้วเท่านั้น (กฎทางธุรกิจ #4)

**Layout**: ใช้ **Word Export Panel** component ตาม DESIGN.md §3 — แสดงเป็น modal/panel ลอย (`radius.lg`, `shadow.card`) เหนือหน้าจอ 8

1. หัวข้อ panel: "ออกเอกสาร Word — [ชื่อวิชา] · [Curriculum Tag]"
2. รายการสรุปสิ่งที่จะรวมในเอกสาร (read-only preview list): CLO/PLO ที่ยืนยันแล้ว, % ความสอดคล้อง, รายชื่อไฟล์หลักฐานที่จะอ้างอิง (เฉพาะที่แนบจริงในระบบ — กฎทางธุรกิจ #4)
3. **Toggle "รวมส่วน Area of Improvement"** (AB-18, ค่าเริ่มต้นเปิด) — ปิดได้ตามความต้องการของแต่ละสถานการณ์ ไม่กระทบความถูกต้องของเนื้อหาหลักฐานหลัก (AC ของ AB-18)
4. ปุ่มหลัก "ดาวน์โหลดเอกสาร Word" (`primary.green`)
5. หมายเหตุท้าย panel (`text.caption`, `text.secondary`): "เอกสารอ้างอิงเฉพาะข้อมูลและหลักฐานที่ยืนยันแล้วในระบบเท่านั้น" — ย้ำกฎทางธุรกิจ #4 ให้อาจารย์เห็นก่อนกดดาวน์โหลดทุกครั้ง

**Curriculum tag**: กำกับที่หัวข้อ panel เสมอ (ป้องกันสับสนเมื่อวิชาเดียวมีหลายกลุ่มหลักสูตรในบางกรณี)

**States**: กำลังสร้างเอกสาร (loading เรียบ), สร้างเสร็จ (ปุ่มเปลี่ยนเป็น "ดาวน์โหลดไฟล์" พร้อม timestamp), มี draft ค้างที่ยังไม่ยืนยัน (แสดง Gap Alert Banner เตือนใน panel นี้ด้วยว่าข้อมูลบางส่วนจะไม่ถูกรวมเพราะยังไม่ยืนยัน — ไม่บล็อกการ export แต่แจ้งให้ทราบล่วงหน้า)

**จุดเชื่อมต่อ**: เปิดจากหน้าจอ 8 (modal ทับหน้าเดิม) → ปิดกลับไปหน้าจอ 8 หลังดาวน์โหลดเสร็จ

---

## ตาราง Traceability: User Story → หน้าจอ/Component (บทบาทอาจารย์ผู้สอน)

| User Story | หน้าจอ | Component หลักที่ใช้แสดงผล |
|---|---|---|
| AB-02 | หน้าจอ 3 | Curriculum Tag, ฟอร์ม CLO |
| AB-03 | หน้าจอ 3 | Gap Alert Banner (เงื่อนไขผูก CLO–PLO), PLO selector ที่กรองตามหลักสูตร |
| AB-05 | หน้าจอ 5 | ฟอร์มบันทึกการสอน, Evidence Attachment List |
| AB-06 | หน้าจอ 5 | Evidence Attachment List (multi-file) |
| AB-07 | หน้าจอ 5 | Evidence Attachment List (เช็คสิทธิ์ก่อนแสดง thumbnail) — ดูฝั่ง audit log ที่ [[align-program-admin-screens#หน้าจอ 5 — ประวัติการเข้าถึงหลักฐาน (Evidence Access Log)|align-program-admin-screens หน้าจอ 5]] |
| AB-08 | หน้าจอ 6 | AI Review Panel + Match % Indicator (Draft) |
| AB-09 | หน้าจอ 6 | PLO chip เชื่อมโยงใต้ CLO |
| AB-10 | หน้าจอ 6 | AI Review Panel (ปุ่มยืนยัน/แก้ไข/ปฏิเสธ) |
| AB-11 | หน้าจอ 1, 8 | Stat Tile, Course/Row List Item |
| AB-12 | หน้าจอ 1 | Gap Alert Banner |
| AB-13 | หน้าจอ 7 | CLO × Week Grid |
| AB-15 | หน้าจอ 9 | Word Export Panel |
| AB-16 | หน้าจอ 8, 9 | ส่วน Area of Improvement, Word Export Panel |
| AB-18 | หน้าจอ 9 | Toggle รวม/ไม่รวม Area of Improvement |
| AB-19 | หน้าจอ 4 | ฟอร์ม/อัปโหลด course syllabus |
| AB-20 | หน้าจอ 1, 6, 8 | Stat Tile, Match % Indicator (สูตรฐาน 100% ต่อวิชา) |
| AB-21 | หน้าจอ 6, 8 | Match % Indicator + ค่าความถี่ที่แมทช์ประกอบกัน |
| AB-22 | หน้าจอ 6, 8 | Gap Alert Banner (missing/extra topics) |
| AB-23 | หน้าจอ 1 | ส่วนเปรียบเทียบ "การสอนจริง vs CLO/syllabus" (แยกจาก AB-11/AB-12) |

---

ต่อยอด/เชื่อมโยง:
- บทบาทที่สอง: [[align-program-admin-screens|align-program-admin-screens]]
- แผนภาพการไปมาระหว่างหน้าจอ: [[align-navigation-flow|align-navigation-flow]]
- pain point และ before/after: [[align-user-journey|align-user-journey]]
- ส่งต่อรายละเอียดเชิงระบบ (data model, API): [[../02-technical/align-technical-design|align-technical-design]]
