# spec.md — สรุปสเปกของสิ่งที่สร้างจริงใน ALIGN (ตามโค้ดปัจจุบัน)

> เอกสารนี้สรุปจากการอ่าน `SCOPE.md` + โค้ดจริงใน `align-app/src` (ไม่ใช่จากเอกสารออกแบบทั้งหมดใน `docs/` เพียงอย่างเดียว — บางส่วนใน `docs/` ยังเป็น "แนวคิด" ที่โค้ดจริงยังไม่ได้ทำ จะระบุแยกไว้ในหัวข้อ 4)
>
> **เกี่ยวกับ SCOPE.md**: ไฟล์นั้นเป็นตารางเทียบชื่อ entity ระหว่างการบ้านคนละโปรเจกต์ (`align-homework-w6`) กับ ALIGN จริง (`nmc-align-2026`) — สรุปสั้นๆ ที่เกี่ยวกับ Module นี้คือ **workflow สถานะ "รอพิจารณา → รับทราบ โดยประธานหลักสูตร" ที่ตารางนั้นสมมติไว้ ไม่ตรงกับของจริง** ของจริงคือ `ai_match_result.state`: `draft` → `confirmed` และ**อาจารย์ผู้สอนเจ้าของวิชาเองเป็นคนเดียว**ที่กดยืนยัน ไม่มีขั้นตอนให้ผู้บริหารหลักสูตรมาอนุมัติบันทึกการสอนเลย

---

## 1. หน้าจอที่มีจริง (Screens)

| Route | หน้าจอ | ใครเข้าได้ |
|---|---|---|
| `/` | หน้าแรก (redirect ตามสถานะล็อกอิน) | ทุกคน |
| `/login` | เข้าสู่ระบบ | ทุกคน (ยังไม่ล็อกอิน) |
| `/register` | สมัครใช้งาน (อาจารย์ผู้สอนเท่านั้น, สถานะเริ่มต้น "รออนุมัติ") | ทุกคน (ยังไม่ล็อกอิน) |
| `/account-status` | สถานะบัญชีของฉัน + ปุ่ม "สลับมุมมองเป็นอาจารย์ผู้สอน" (เฉพาะ program_admin) | ผู้ล็อกอินทุกสถานะ (pending/approved/rejected) |
| `/dashboard` | แดชบอร์ดหน้าแรก — เนื้อหาต่างกันตามบทบาท (ดูหัวข้อ 1.1/1.2) | approved เท่านั้น |
| `/courses` | รายการวิชา (อาจารย์เห็นเฉพาะของตน, program_admin เห็นทั้งหมด) | approved เท่านั้น |
| `/courses/new` | เพิ่มรายวิชาใหม่ (เลือกจาก catalog จริงผ่าน dropdown ไม่พิมพ์เอง) | approved เท่านั้น |
| `/courses/[curriculumId]/[code]` | จัดการรายวิชา — ตั้งค่า CLO, ผูก CLO↔PLO (พร้อม AI ช่วยแนะนำ), การ์ดสรุป coverage, ปุ่ม "ให้ AI สรุปภาพรวมวิชา" (Area of Improvement), ลิงก์ไปหน้า Syllabus/CLO×สัปดาห์/ดาวน์โหลด Word | เจ้าของวิชา (instructor) แก้ไขได้; program_admin ดูได้อย่างเดียว |
| `/courses/[curriculumId]/[code]/syllabus` | กำหนดหัวข้อที่วางแผนสอนรายสัปดาห์ (1–15) — ใช้เป็น context ให้ AI เทียบกับการสอนจริง (AB-19) | เจ้าของวิชาเท่านั้น |
| `/courses/[curriculumId]/[code]/clo-week-map` | ตารางอ่านอย่างเดียว: CLO × สัปดาห์ (1–15) แสดงจำนวนหลักฐานยืนยันแล้วต่อสัปดาห์ + รวมสะสม + PLO ที่ผูก (AB-13) | เจ้าของวิชาเท่านั้น |
| `/courses/[curriculumId]/[code]/teaching-records/new` | บันทึกการสอนจริงรายสัปดาห์ (หัวข้อ, สัปดาห์ 1–15, ภาคเรียน/ปีการศึกษา, แนบไฟล์หลักฐานได้หลายไฟล์ ไฟล์ละไม่เกิน 4MB) พร้อม dialog เตือนถ้าสัปดาห์นั้นเคยบันทึกไว้แล้ว | เจ้าของวิชาเท่านั้น |
| `/courses/[curriculumId]/[code]/teaching-records/[recordId]/review` | ตรวจ/ยืนยันผลจับคู่ AI (CLO ที่จับคู่ได้, เหตุผล, ประวัติการรัน agent) + รายการไฟล์หลักฐานที่แนบ ก่อนเป็นข้อมูลจริง | เจ้าของวิชาเท่านั้น |
| `/curricula/[curriculumId]/plos` | จัดการ PLO ของหลักสูตร (เพิ่ม/แก้ไขคำอธิบาย/soft-delete) | program_admin ที่ curriculum นั้นอยู่ใน scope เท่านั้น |
| `/admin/accounts` | อนุมัติ/ปฏิเสธบัญชีผู้สมัคร + ดูรายชื่อบัญชีทั้งหมด | program_admin เท่านั้น |
| `/courses/[curriculumId]/[code]/export-word` | (Route Handler ไม่ใช่หน้าจอ) ดาวน์โหลดเอกสาร Word สรุปรายวิชา | เจ้าของวิชา หรือ program_admin ที่ curriculum นั้นอยู่ใน scope |
| `/dashboard/export-program-word` | (Route Handler) ดาวน์โหลดเอกสาร Word สรุประดับหลักสูตร (สำหรับ AUN-QA) | program_admin ที่ curriculum นั้นอยู่ใน scope เท่านั้น |
| `/evidence/[evidenceId]/download` | (Route Handler) ดาวน์โหลดไฟล์หลักฐาน (เก็บบน Vercel Blob แบบ private) — ทุกครั้งที่เข้าถึงสำเร็จจะบันทึกลง `evidence_access_logs` (PDPA, AB-07) | เจ้าของวิชา หรือ program_admin ที่ curriculum นั้นอยู่ใน scope เท่านั้น |

### 1.1 `/dashboard` มุมมองอาจารย์ผู้สอน (AB-11)
% ความสอดคล้องรวม + รายชื่อวิชาที่สอนทั้งหมด พร้อมกลุ่มหลักสูตร (2565/2570) — ไม่ล็อกด้วยจำนวนสัปดาห์ที่สอนแล้ว (สั่งวิเคราะห์ได้ทุกเมื่อ)

### 1.2 `/dashboard` มุมมองผู้บริหารหลักสูตร (AB-14)
แท็บแยกตามหลักสูตรที่ตนดูแล **เท่านั้น** (หลักสูตรนอก scope จะไม่แสดงแท็บเลย ไม่ใช่แสดงแล้ว disable — กัน leak ข้อมูลตาม PDPA) ต่อแท็บ: ตัวกรองภาคเรียน/ปีการศึกษา (optional), % coverage เฉลี่ยของทุกวิชา, รายวิชาพร้อม % และ CLO ที่ขาดหลักฐาน, ตารางระดับ PLO (สัดส่วน CLO ที่ผูกแล้วมีหลักฐาน), ปุ่ม "ให้ AI สรุปภาพรวมหลักสูตร" + Area of Improvement, ลิงก์ดาวน์โหลด Word ระดับหลักสูตร

---

## 2. โครงสร้างข้อมูล (Firestore Collections ที่โค้ดจริงใช้)

```
curricula/{curriculum_id}                 doc id = "2565" | "2570"
  plos/{plo_id}                           doc id = code เช่น "PLO1"
  courses/{course_id}                     doc id = รหัสวิชาจริง เช่น "127121"
    clos/{clo_id}                         doc id = code เช่น "CLO1"
    syllabus/main                         เขียนได้จริงแล้วผ่านหน้า .../syllabus (AB-19), agent อ่านค่านี้ประกอบการวิเคราะห์
  clo_plo_mappings/{mapping_id}           doc id = "{course_id}_{clo_code}_{plo_code}"
  program_overview_logs/{log_id}          ผลสรุปจาก AI ระดับหลักสูตร (AUN-QA), เก็บ semester/academic_year ที่ query ตอนนั้น

teaching_records/{record_id}              top-level, auto id
  agent_logs/{log_id}                     ประวัติการรัน AI matching agent ทุกครั้ง (สำเร็จ/ล้มเหลว)

users/{user_id}                           doc id = Firebase Auth UID
account_approval_logs/{log_id}            top-level, append-only
ai_match_results/{match_result_id}        top-level, auto id, state: draft_ai_pending → confirmed
overview_logs/{log_id}                    ผลสรุป AI ระดับรายวิชา (Area of Improvement ต่อวิชา)
evidence/{evidence_id}                    top-level, auto id — ไฟล์แนบจริง เก็บบน Vercel Blob (private), field ชี้ path คือ blob_pathname
evidence_access_logs/{log_id}             top-level, append-only — บันทึกทุกครั้งที่มีการดาวน์โหลดไฟล์หลักฐานสำเร็จ (PDPA, AB-07)
```

**field สำคัญที่ต้องรู้**:
- `course`: `instructor_id` (ต้องเป็น UID ของบัญชี instructor ที่มีจริงเท่านั้น — ตรวจกับ `users` ก่อนบันทึกเสมอ), `clo_plo_ready` (คำนวณสดจาก transaction ทุกครั้งที่ผูก/ปลด CLO↔PLO ไม่ได้ set ตรงๆ)
- `clo`: `code`, `description`, `is_deleted` (soft delete)
- `clo_plo_mapping`: doc id แบบ composite กันผูกซ้ำโดยอัตโนมัติ (ใช้ `create()`)
- `teaching_record`: `course_id`, `curriculum_id`, `topic`, `week_no` (1–15), `semester`, `academic_year` (2 field หลังเป็นของใหม่ที่ไม่มีในสเปกเดิม, บันทึกเก่าก่อนหน้านี้จะเป็น `null`), `is_deleted`
- `ai_match_result`: `state` (`draft_ai_pending` | `confirmed`), ผลจับคู่ CLO + เหตุผล — **AI ไม่เคยเขียนสถานะ `confirmed` เอง มีแค่อาจารย์เจ้าของวิชากดยืนยันเท่านั้น** (กฎ #3)
- `evidence`: `teaching_record_id`, `course_id`, `curriculum_id`, `file_name`, `blob_pathname` (private, เข้าถึงผ่าน `/evidence/{id}/download` เท่านั้น ไม่ใช่ URL สาธารณะ), `uploaded_by`, `is_deleted` — **เก็บบน Vercel Blob ไม่ใช่ Firebase Cloud Storage** (เบี่ยงเบนจากสเปกเดิมโดยเจตนา เพราะ Firebase Storage ต้อง upgrade เป็น Blaze plan แต่ Vercel Blob ใช้ฟรีได้ในระดับนี้ ไม่ต้องผูกบัตร)

---

## 3. บทบาทผู้ใช้ (Roles)

มี **2 บทบาทเท่านั้น** เก็บที่ `users/{uid}.role` — ไม่มีบทบาทที่ 3 (เช่น QA) โดยเจตนา:

> **กันสับสนกับ "ประธานหลักสูตร" ใน `SCOPE.md`**: ชื่อนั้นเป็น role ของอีกโปรเจกต์ (`align-homework-w6`) ที่แยกไว้ต่างหาก ไม่ใช่ role ในระบบ ALIGN จริง — ไม่มี role ชื่อนี้ใน `users/{uid}.role` เลย และ workflow ที่ผูกกับชื่อนั้น ("รอพิจารณา → รับทราบ โดยประธานหลักสูตรกดอนุมัติทีละบันทึกการสอน") ก็ไม่ตรงกับของจริง — ของจริงคืออาจารย์ผู้สอนเจ้าของวิชาเองเป็นคนเดียวที่กดยืนยันผล AI (`ai_match_result.draft → confirmed`) ไม่มีขั้นตอนให้ `program_admin` มาอนุมัติบันทึกการสอนแทน

1. **อาจารย์ผู้สอน (instructor)** — สมัครเองผ่าน `/register`, กรอก CLO/ผูก PLO/บันทึกการสอน/ยืนยันผล AI ของ**วิชาที่ตนสอนเท่านั้น**
2. **ผู้บริหารหลักสูตร (program_admin)** — สร้างได้เฉพาะผ่าน seed script (ไม่มีหน้าสมัคร), อนุมัติ/ปฏิเสธบัญชี, ดูภาพรวมหลักสูตรที่ตนดูแล (`program_admin_curriculum_scope`), มอบหมาย/ลบวิชาได้ทุกวิชา — **แก้ไข/ยืนยันผล AI ของวิชาใดๆ ไม่ได้** แม้เป็นผู้ดูแลหลักสูตรนั้นก็ตาม

**ส่วนขยายที่ไม่มีในสเปกเดิม (เบี่ยงเบนที่ยืนยันแล้ว)**: program_admin กดปุ่มที่ `/account-status` เพื่อ "สลับมุมมองเป็นอาจารย์ผู้สอน" ชั่วคราวได้ (ใช้ cookie ไม่แก้ role จริงใน Firestore) เพื่อกรอกข้อมูลแทนตนเองในกรณีที่ตนเองก็สอนด้วย — **ทางเดียว**: อาจารย์ผู้สอนสลับเป็นผู้บริหารหลักสูตรไม่ได้เด็ดขาด

รายละเอียดสิทธิ์แบบตารางเต็ม ดูที่ `ACL.md`

---

## 4. สิ่งที่ "ไม่ทำ" ใน Module นี้ (นอกสโคปจริงของโค้ดปัจจุบัน)

รายการนี้คือฟีเจอร์ที่**มีอยู่ในเอกสารสเปก/backlog แต่ยังไม่ถูกสร้างเป็นโค้ดจริง** หรือ**ตั้งใจไม่ทำในระบบนี้เลย**:

1. **AI วิเคราะห์ gap เทียบ syllabus แบบมีโครงสร้างเฉพาะ — AB-22** — ตอนนี้ agent อ่าน syllabus จริงประกอบการสรุปแล้ว (ปิดช่องว่างเดิมที่เคยว่างเปล่าตลอด) แต่ยังไม่มีเอาต์พุตแบบมีโครงสร้างที่ระบุแยกชัดเจนเป็น "หัวข้อที่ขาด" vs "หัวข้อที่สอนเพิ่ม" ตามที่ backlog ตั้งใจ (ไม่มี `syllabus_gap_result` collection แยก) — ปัจจุบันข้อมูลนี้ปนอยู่ในข้อความสรุปอิสระของ AI เท่านั้น
2. **ระบบแจ้งเตือนอัตโนมัติ (Notification) — AB-12, AB-23** — **ตัดสินใจแล้วว่าจะไม่ทำแยก** (ผู้ใช้ยืนยัน 2026-09-19) เพราะโปรเจกต์นี้ไม่มี Cloud Functions/background job ให้ trigger แบบ "ทันที" ได้จริง แทนที่ด้วยการให้ผู้ใช้เห็น gap ผ่านหน้าที่มีอยู่แล้ว (หน้าจัดการรายวิชา, แดชบอร์ด, แผนที่ CLO×สัปดาห์ ที่แสดง "ยังไม่มีหลักฐาน" อยู่แล้ว) — ไม่มี collection `notifications` และจะไม่มีในอนาคตอันใกล้ตามการตัดสินใจนี้
3. **บทบาทที่ 3 (QA / ผู้ประเมินภายนอก)** — ตั้งใจไม่ทำโดยเจตนา ตามสเปค QA **ไม่มี login เข้าระบบเอง** ได้รับแค่เอกสาร Word ที่ export ออกไปเท่านั้น (AB-17)
4. **การให้คะแนน/ตัดสิน "บรรลุ PLO" แบบผ่าน/ไม่ผ่าน** — ไม่มีสูตรตายตัวในสเปก และตั้งใจไม่ทำเป็นเกณฑ์ฟันธง — ระบบแสดงเป็น**สัดส่วน %** เท่านั้น ให้อาจารย์/ผู้บริหารตีความเอง (AI ก็ถูกสั่งห้ามฟันธงในคำตอบเช่นกัน)
5. **PDF export** — ไม่มีในสเปกที่ไหนเลย ระบบ export เป็น **Word (.docx) เท่านั้น**
6. **AI เป็นผู้ตัดสินใจ/บันทึกข้อมูลจริงเอง (autonomous write)** — ไม่ทำโดยเจตนา (กฎธุรกิจ #3): ทุกผลจาก AI (จับคู่ CLO/PLO, สรุปภาพรวม, ข้อเสนอแนะ) เป็นแค่ **draft/ข้อเสนอแนะ** เสมอ ต้องให้อาจารย์ผู้สอนเจ้าของวิชากดยืนยันเองก่อนนับเป็นข้อมูลจริงทุกครั้ง ไม่มีทางลัดให้ AI auto-confirm ได้
7. **การ query/รวมข้อมูลข้ามกลุ่มหลักสูตร 2565↔2570** — ห้ามโดยเด็ดขาดในทุกกรณี แม้แต่ program_admin ที่ดูแลทั้งสองหลักสูตรก็เห็นแยกเป็นคนละแท็บ ไม่มีมุมมองรวมยอดข้ามหลักสูตร
8. **ไฟล์ syllabus ทางการ (official syllabus file upload)** — ทำเฉพาะส่วน "หัวข้อรายสัปดาห์" (ข้อความ) เท่านั้น ไม่ได้ทำการอัปโหลดไฟล์ syllabus ฉบับทางการ (ทำได้จริงแล้วถ้าต้องการ เพราะมี Vercel Blob ใช้งานได้แล้วจากฟีเจอร์แนบหลักฐาน — แค่ยังไม่ได้ต่อยอดมาที่จุดนี้)

**ปิดแล้ว (เดิมเคยอยู่ในรายการนี้ ตอนนี้ implement จริงแล้ว)**: แนบไฟล์หลักฐาน + PDPA access log (AB-05/06/07, ใช้ Vercel Blob แทน Firebase Storage — ดูหัวข้อ 2), กรอก Course Syllabus (AB-19), แผนที่ CLO×สัปดาห์ (AB-13), จัดการ PLO ผ่าน UI (เพิ่ม/แก้ไข/soft-delete)

---

*เอกสารนี้สะท้อนสถานะโค้ดจริง ณ วันที่ 2026-09-19 (อัปเดตหลังสร้าง PLO CRUD, Course Syllabus, CLO×สัปดาห์ matrix, และแนบไฟล์หลักฐานจริงผ่าน Vercel Blob) — ถ้ามีการเพิ่มฟีเจอร์ใหม่ภายหลัง ต้องอัปเดตหัวข้อ 4 ตามไปด้วย*
